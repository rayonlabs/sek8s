# GPU verification configuration
{% if debug_build | default(false) %}
# Debug builds: a GPU verification failure must NOT power off the VM — you can't
# debug a VM that shuts itself down. gpu-verify.sh's fatal() then warns and continues.
GPU_VERIFY_DEBUG_MODE=true
{% endif %}
