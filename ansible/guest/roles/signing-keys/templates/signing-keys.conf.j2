# /etc/chutes/signing-keys.conf — copied into the initramfs by the
# fetch-signing-keys hook.  Provides the API URL for the key bundle fetch.
# This file is under /etc/chutes/ and is therefore measured into RTMR3.
SIGNING_KEYS_URL={{ validator_base_url }}/servers/signing-keys
